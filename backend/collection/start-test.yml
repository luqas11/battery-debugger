info:
  name: start-test
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/start-test"
  body:
    type: json
    data: |-
      {
        "name": "Test1",
        "date": "2026-06-14",
        "current": 0.25,
        "age": 12
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
