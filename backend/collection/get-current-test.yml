info:
  name: get-current-test
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/get-current-test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
