info:
  name: get-test-list
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/get-test-list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
