// Copy this file to config.h and fill in your values.
// config.h is gitignored and never committed.

// Credentials for the setup access point the ESP exposes to configure WiFi.
// The password must be at least 8 characters (WPA2 requirement).
#define AP_SSID     "SSID"
#define AP_PASSWORD "PASSWORD"
